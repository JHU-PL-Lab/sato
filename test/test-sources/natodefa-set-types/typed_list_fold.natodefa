# EXPECT-WELL-FORMED
# EXPECT-NO-TYPE-ERRORS

let rec fold (acc : int) (foldf : int -> int -> bool) (lst : [int]) : int =
    match lst with
    | [] -> acc
    | hd :: tl -> fold (foldf acc hd) foldf tl
    end
in
fold
