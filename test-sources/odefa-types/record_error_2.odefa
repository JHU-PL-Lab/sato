# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR-COUNT 1
# EXPECT-TYPE-ERROR ~result [] : "match" "r" "{ zero = z, one = o }" "s = r z" "fun" "{zero, one}"

z = 0;
o = 1;
r = { zero = z, one = o };
s = r z;
t = s + s;
