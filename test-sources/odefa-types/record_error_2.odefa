# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR-COUNT 1
# EXPECT-TYPE-ERROR ~result [] : "match" "rec" "{ x = zero, y = one }" "ans = rec.z" "{z}" "{x, y}"

zero = 0;
one = 1;
rec = { x = zero, y = one };
ans = rec.z;
