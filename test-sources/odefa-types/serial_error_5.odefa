# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR ~result [] : [ "x = zero = 0" "rx = x and tru" "bool" "int" ] [ "y = one = 1" "ry = y or fls" "bool" "int" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

zero = 0;
one = 1;
tru = true;
fls = false;

f = fun x -> ( rx = x and tru );
g = fun y -> ( ry = y or fls );

a = f zero;
b = g one;
c = a and b; # knock-on error!
