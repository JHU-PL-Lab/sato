# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR-COUNT 2
# EXPECT-TYPE-ERROR ~result [] : "match" "x" "zero = 0" "rx = x and tru" "bool" "int"
# EXPECT-TYPE-ERROR ~result [] : "match" "y" "one = 1" "ry = y or fls" "bool" "int"

zero = 0;
one = 1;
tru = true;
fls = false;

f = fun x -> ( rx = x and tru );
g = fun y -> ( ry = y or fls );

a = f zero;
b = g one;
c = a and b; # knock-on error!
