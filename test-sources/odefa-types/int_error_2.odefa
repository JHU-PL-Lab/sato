# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR ~result [] : [ "b = tru and fls" "c = a + b" "int" "bool" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

zero = 0;
one = 1;
tru = true;
fls = false;

z = zero;

a = zero + one;
b = tru and fls;
c = a + b;
