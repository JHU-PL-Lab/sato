# EXPECT-WELL-FORMED
# EXPECT-TYPE-ERROR-COUNT 1
# EXPECT-TYPE-ERROR ~result [] : "match" "y" "c = true" "sum = x + y" "int" "bool"
# # EXPECT-TYPE-ERROR ~result [] : "match" "y" "b = {}" "sum = x + y" "int" "{}"

# XXX: Interesting that both type errors can be detected

a = 2;
b = {};
c = true;
f = fun x -> (
    fret = fun y -> ( sum = x + y; );
    );
f1 = f a;
m = f1 c;
f2 = f a;
n = f2 b;
target = m;
