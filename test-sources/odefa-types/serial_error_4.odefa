# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR ~result [] : [ "x = zero = 0" "rx = x and tru" "bool" "int" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

zero = 0;
one = 1;
tru = true;
fls = false;

f = fun x -> ( rx = x and tru );
g = fun y -> ( ry = y or fls );

a = f zero;
b = g a; # knock-on error!
c = b xor tru; # not b
