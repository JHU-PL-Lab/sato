# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR ~result [] : [ "not_rec = fun x -> ( ret = x )" "ans = not_rec.label" "{label}" "fun" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

not_rec = fun x -> ( ret = x );
ans = not_rec.label;
