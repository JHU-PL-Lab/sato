# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR ~result [] : [ "x = zero = 0" "r = x and tru" "bool" "int" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

zero = 0;
one = 1;
tru = true;

f = fun x -> ( r = x and tru );
fans = f zero;

z = fans + one; # knock-on error!
