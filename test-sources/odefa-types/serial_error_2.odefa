# EXPECT-WELL-FORMED
# EXPECT-TYPE-ERROR ~result [0, 0] : [ "b = true" "sum1 = a + b" "int" "bool" ] [ "b = true" "sum3 = sum1 + b" "int" "bool" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

a = input;
b = true;
sum1 = a + b;
c = input;
sum2 = sum1 + c;
sum3 = sum1 + b;
