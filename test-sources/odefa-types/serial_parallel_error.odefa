# EXPECT-WELL-FORMED
# EXPECT-TYPE-ERROR ~result [0, -1] : [ "b = false" "sum1 = a + b" "int" "bool" ] [ "x = t = true" "rf = x + o" "int" "bool" ]
# EXPECT-TYPE-ERROR ~result [0, 0] : [ "b = false" "sum1 = a + b" "int" "bool" ] [ "y = z = 0" "rg = y and t" "bool" "int" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

z = 0;
o = 1;
t = true;

f = fun x -> ( rf = x + o );
g = fun y -> ( rg = y and t );

a = input;
b = false;
sum1 = a + b;
c = input;
bul = c < z;
res = bul ? ( r1 = f t ) : ( r2 = g z );
target = res;
