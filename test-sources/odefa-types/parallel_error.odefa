# EXPECT-WELL-FORMED
# EXPECT-TYPE-ERROR-COUNT 2
# EXPECT-TYPE-ERROR ~result [0] : "match" "" "a = true" "rf = a + b" "int" "bool"
# EXPECT-TYPE-ERROR ~result [1] : "match" "" "b = 1" "rg = a or b" "bool" "int"

a = true;
b = 1;
i = input;
zero = 0;
f = fun x -> ( rf = a + b );
g = fun y -> ( rg = a or b );
bul = i == zero;
z = bul ? ( r1 = f zero ) : ( r2 = g zero );
